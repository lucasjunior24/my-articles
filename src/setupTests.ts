/**
 * Setup file for Vitest + Testing Library (11.1.2).
 *
 * Extends Vitest's expect with jest-dom matchers
 * (e.g., toBeInTheDocument, toHaveTextContent, toHaveAttribute).
 */
import "@testing-library/jest-dom/vitest";
